# GraphQL Schema Design (Condensed)

- Prefer schema-first design.
- Use strong typing and explicit nullability.
- Use input/payload pattern for mutations.
- Prevent N+1 using DataLoader.
- Enforce query depth and complexity limits.
- Apply cursor pagination for large datasets.
- Mark deprecated fields instead of breaking removals.
- Document schema fields and error patterns.
